追击战斗系统。
暂时全写在主模块里了，以后有时间慢慢挪出来。
相关函数：
\battle_wrapper\battle_prepare(&$pa, &$pd, $active)
\battle_wrapper\battle_finish(&$pa, &$pd, $active)
\battle_wrapper\battle_wrapper(&$pa, &$pd, $active)
\battle_wrapper\get_battle_distance(&$pa, &$pd, $active)
\weapon\check_can_counter\(&$pa, &$pd, $active)
\enemy\meetman_once_again(&$edata,$log_kind=0)
\enemy\meetman_then_escape(&$edata)
\act()